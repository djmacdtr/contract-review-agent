# Text 单批证据回查失败快速路径

日期：2026-08-27

## 只读核查结论

任务 `tsk_01M113Z6XJAAF7AFE41HPV8YQF` 的失败已经精确定位，不需要再次通过完整三文件任务调试。

- 失败文件：参考资料《项目方案确认函.docx》
- chain：`text`
- batch_id：`batch_f194945649fc1f6e0613557c`
- batch_depth：`1`
- unit_count：`1`
- failure_code：`FACT_VALUE_NOT_GROUNDED`
- 当前任务已保存：`3` 条 profile、`97` 条 numeric、`47` 条 text 成功 checkpoint

该错误表示模型返回了无法在指定结构单元原文中严格回查的非数值事实。客户端已经允许单结构证据纠错，但纠错后仍未通过；当前控制器又将这个单条候选错误升级为整个合同任务失败。

## 最短处理路径

1. 新增/扩展只读 text 单批诊断入口，按上述 task_id、file_id 和 batch_id 重建并只调用该批次，禁止下载、OCR、页码服务、其他抽取链和正式任务创建。
2. 非数值事实响应改为逐项证据验证：保留严格回查成功的候选，丢弃无法回查的候选并记录内部计数；不得把无依据候选发布为事实。
3. 一个结构单元允许产生零条事实。全部候选均无法回查时，该批次以空事实成功完成，不再导致整份合同失败。
4. 该降级只降低召回，不引入未经证据支持的差异；依赖被丢弃事实的检查不得生成“校验通过”。
5. 只运行证据过滤相关定向测试，再执行一次上述精确 batch 诊断。通过后仅 retry 最新任务，复用其 147 条事实 checkpoint。

本记录只进行了数据库、配置和代码只读核查；未修改业务代码，未调用 LLM/OCR，未创建或重试任务。
