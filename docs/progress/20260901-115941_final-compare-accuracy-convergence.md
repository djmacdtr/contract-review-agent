# FINAL_COMPARE V2 准确率收敛记录

## 本次范围

- 仅调整 `FINAL_LOGICAL_V2` 的正文语义对齐、表格业务键配对和不确定候选展示。
- `LEGACY FINAL_COMPARE`、`DRAFT_REVIEW`、公开接口、数据库结构和历史结果未改为新语义。
- 未调用 OCR、LLM、任务创建接口，也未写入数据库；`.real-diagnostic-temp/` 保持不变。

## 实现结果

- 条款/列表比较增加可选的语义标题锚点：仅在正文去除结构编号后完全一致时吸收编号偏移；交叉引用或正文实际变化仍保留。
- 连续段落删除可聚合，局部相邻的新增/删除可在相似且同侧相邻时重组为 `MODIFIED`，并保留双方物理位置。
- V2 表格按规范化表头和业务键配对，并使用纵向合并单元格作为行身份上下文；不改变单元格比较本身。
- V2 中结构无法可靠确认的候选不进入正式 `diff_items/risk_items`，而进入带完整证据的 `review_items`；公开复核证据同样受页码门禁约束。
- 新增脱敏金标分类样本：`tests/fixtures/final_compare_gold/first_pair_deidentified.json`；不保存合同正文。

## 离线验证

- 定向 pytest：`108 passed`（V2、比较、工作流、DOCX 页码相关）。
- 变更范围 Ruff：通过。
- 相关 Python `compileall`：通过。
- `git diff --check`：通过（仅有 Git 的 LF/CRLF 提示）。
- cache-only dry-run：`SUCCEEDED`；OCR=0、LLM=0、数据库写入=0。

## 首组安全统计

- 来源历史报告差异数：189。
- 当前 V2 原始候选数：139；V2 `review_required`：70。
- 完全重复签名组：0；同位置跨类型重复组：0。
- 双侧规范化文本不同：65；数值变化：9；金额/日期/期限/编号上下文变化计数：20/2/5/4。
- 对齐覆盖率：baseline `0.709`，target `0.9222`；对齐可靠性：`true`。
- 公开证据页码：`300/300`，缺失 `0`。
- 真实重复簇：0；重复簇 Canary：`SKIPPED_NO_CANDIDATES`，LLM=0。

## 未完成项

- 当前 OCR 输入下未发现可安全证明的真实重复簇，因此没有执行外部 Canary 或正式任务。
- 首组真实文件的逐项人工金标仍需由业务方对第 23/26 页及疑似错配区域确认；仓库只保留脱敏分类和安全统计。
- 若人工确认存在表格配对错误，本阶段仅记录并另行制定映射裁决方案，不通过重复簇模型删除候选。
