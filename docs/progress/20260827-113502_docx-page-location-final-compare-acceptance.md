# DOCX 公开证据页码映射修复与 FINAL_COMPARE 进度记录

## 结论

页码映射修复已完成，唯一一次新的宿主机 FINAL_COMPARE 已成功完成并持久化结果。接口级结果校验通过；控制台可见页面验收未完成，因为当前浏览器连接没有可用实例。本记录不把接口校验冒充控制台验收，因此本轮未创建提交。

## 安全结构诊断

首次安全诊断只记录结构 ID、类型、索引、候选页和邻接结构，不记录合同正文、印章文字、图片 URL、Base64、鉴权头或完整 OCR 响应。

| 结构 ID | 类型/索引 | 诊断分类 | 确定性页码证据 | 绑定页 |
| --- | --- | --- | --- | --- |
| `paragraph:223` | 段落，索引 223 | 合并/结构范围 | 前一段落与后一张表均为第 17 页 | 17 |
| `paragraph:227` | 段落，索引 227 | 合并/结构范围 | 前一段落与后一张表均为第 18 页 | 18 |
| `paragraph:254` | 段落，索引 254 | 分页边界 | 前一段落为第 22 页，紧邻唯一表格锚点为第 23 页，页号连续 | 23 |
| `paragraph:256` | 段落，索引 256 | 合并/结构范围 | 前一段落与后一张表均为第 24 页 | 24 |

四项均为普通段落，不属于表格单元格；安全诊断未发现重复文本候选或显式分页标记。回退只消费 OCR 返回的真实页码，不使用段落比例、文档总页数或固定页码。

修复后的宿主机诊断结果为：外部页数 25，527/527 本地结构已绑定，未映射 0，覆盖率 1.0。

## 唯一正式任务

- 任务 ID：`tsk_01M10M5MBS75XAV7PBMB5YGWW2`
- 角色：DOCX 为 `BASELINE`，盖章 PDF 为 `TARGET`
- 尝试次数：1；未 retry、未原样重跑、未创建第二个新任务
- 最终状态：`SUCCEEDED / COMPLETED / 100`
- 结果模式：非 Mock、确定性规则比对
- 文件页数：DOCX 25 页，盖章 PDF 27 页
- 差异数量：189；包含新增、删除、修改、数值变化、页面缺失、内容块缺失和表格单元格/行差异
- 局部证据段操作：`EQUAL`、`DELETE`、`INSERT` 均存在；所有接口返回的证据页均在对应文件真实页数范围内
- AI 建议：189 个风险项均有一句分析建议，整体建议存在
- 校验通过项：结果返回 0 项；报告协议仍包含通过项区域，未伪造通过结论
- 印章影像：22 项，按文件/页/图片内容去重后仍为 22 项，页集合为 19、20、21、23、24、25、27；均为受限 PNG/JPEG Base64，未返回上游 URL

## 安全与接口校验

- DOCX 页码绑定在解析阶段写入内部结构元数据，结构 ID 和物理页集合在公开结果序列化时排除。
- 公开证据页码由已绑定位置传播，持久化阶段不再用正文重新匹配；无法唯一确定的合成测试仍保持 `DOCX_PAGE_LOCATION_INCOMPLETE` 门禁。
- OCR 印章对象只在放款阶段盖章 PDF 请求；未开启全局图片返回，未暴露 OCR API Key 或鉴权头。
- 图片 URL 直达探针未作为浏览器承载方式；结果只使用后端受限物化的 PNG/JPEG Base64，并保留数量、单图大小和总数据量限制。

## 验证结果

- 相关后端测试：127 passed，包含页码映射、路由、比较、FINAL_COMPARE、OCR/印章、结果协议和通过项测试。
- 前端：印章影像相关测试、格式测试、typecheck、build 均通过。
- 变更范围 Ruff：通过。
- `compileall`：通过。
- `git diff --check`：通过（仅有既有换行格式提示）。
- API 镜像已重建并刷新；`/health`、`/ready` 正常。

## 控制台验收状态

接口级检查已确认文件名/页数、差异类型、局部高亮所需段操作、AI 建议、真实页码和印章去重结果。尝试连接控制台时浏览器发现结果为空，没有可用浏览器实例，因此未能执行可见页面的 Tab 切换、图片自然尺寸和页面技术字段检查。按要求没有使用独立浏览器自动化或源码绕过，也没有将该部分标记为通过。

## 环境恢复

- 宿主机 Worker 和临时只读文件服务已停止；临时服务端口 18081 已释放。
- Docker Worker 已恢复运行；PostgreSQL 仍使用 `127.0.0.1:15432` 映射，API、数据库和配置就绪检查正常。
- 正式 `.env` 未写入 `127.0.0.1` 临时白名单；临时地址未保留在交付配置中。
- `.real-diagnostic-temp/` 未清理；正式运行安全日志保留在 `tmp/formal-final-compare/`。
- 未执行 commit、push、reset，也未清理其他会话资产。
