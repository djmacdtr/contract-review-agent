# 甲方 OCR 印章识别能力检查

- 时间：2026-08-27 09:28（Asia/Shanghai）
- 文件：`04 合同素材文件/03 合同盖章版本/金坛东旭农业-保证合同.pdf`
- 文件属性：8 页、未加密
- 测试方式：同步 OCR，`parse_mode=scan`、`get_image=objects`、图片返回类型为资源引用
- 调用次数：1 次，零重试
- 安全约束：未记录 API 密钥、印章文字、合同正文、图片 URL 或完整上游响应

## 调用结果

- HTTP 状态：200
- 业务码：200
- 有效页数：8/8
- 耗时：约 27.0 秒
- 响应大小：约 455 KB

## 印章识别结果

- OCR 将印章标记为 `type=image`、`sub_type=stamp`。
- 在 `detail` 中发现 12 个印章图片对象，分布于第 1、3、5、7、8 页。
- `pages` 结构中包含对应的 12 个重复对象，因此递归统计得到 24 个节点；实际应按同页位置去重，约为 12 个印章或骑缝印章片段。
- 第 8 页识别到 4 个完整印章/签章对象；其他页面主要为合同边缘的骑缝印章片段，与页面渲染结果一致。
- 12 个印章对象的 `text` 字段均为空，未作为可直接使用的印章文字返回。
- `detail` 对象提供图片 URL 和位置；`pages` 对象提供图片资源路径、裁剪区域和位置坐标。
- 本次使用资源引用模式，因此没有返回 base64 图片正文。

## 结论

甲方 OCR 具备印章检测和定位能力，但本次样本中印章以图片对象返回，并未返回印章文字识别结果。若后续需要展示印章，可使用图片资源和坐标；若需要提取印章单位名称，则还需要对裁剪后的印章图片执行专门的印章文字识别或视觉模型分析。

当前项目固定参数为 `get_image=none`，且项目范围明确不实现印章检查。本轮仅验证甲方接口能力，未修改业务代码、公开接口或交付配置，未执行 commit、push 或创建业务任务。
