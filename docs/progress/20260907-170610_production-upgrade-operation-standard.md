# 任务进度：生产离线升级操作规范

## 基本信息

- 时间：2026-09-07 17:06:10 +08:00
- 状态：COMPLETED
- 任务类型：DOCS
- 代码目录：`D:\work\contract_review\contract-review-agent`
- 当前分支：`feat/draft-review-multidoc`
- 当前提交：`1a7482e`
- 工作树状态：dirty；保留既有未跟踪历史验收 JSON，新增部署操作说明和 README 入口

## 用户目标

将本次在甲方服务器已验证成功的离线升级方式固化为以后统一使用的操作规范，避免每次重新整理手工步骤。

## 本次完成

- 新增统一的甲方生产离线升级操作说明。
- 在根 README 的 Docker 章节增加生产升级说明入口。
- 固化三文件上传、外层/包内 SHA-256、自动升级、健康检查、回滚和禁止清理数据库卷等规则。
- 规范以 `deploy_offline_upgrade.sh` 在 `zjzl89` 的实际成功结果为依据。

## 修改文件

- `docs/deployment/甲方生产离线升级操作说明.md`：新增生产升级标准流程。
- `README.md`：链接生产升级标准，区分开发首次启动与生产增量升级。

## 接口、数据和配置变化

- API：无变化。
- 数据库/迁移：无变化；说明要求保持 `0003_extraction_checkpoint (head)`。
- 配置：无生产配置修改；说明明确保留服务器现有 `.env`。
- 兼容性：适用于现有 openEuler/Linux x86_64 Docker Compose 部署。

## 测试与验证

| 命令/检查 | 结果 | 关键数字或说明 |
|---|---|---|
| `git diff --check` | 通过 | 无新增空白错误 |
| 文档与 README 交叉检查 | 通过 | 统一指向自动化升级入口 |
| 甲方实际升级结果 | 通过 | `11f87a2` 已在 `zjzl89` 成功部署 |

## Docker 与运行状态

- API/Worker：保持甲方已升级后的 `contract-review-agent:11f87a2`。
- PostgreSQL：既有数据库卷保留。
- 控制台：未进行视觉验收。
- 本地 Docker：未因本次文档修改重启服务。

## 重要决策

- 后续生产升级统一上传升级包、外层 SHA 文件和 `deploy_offline_upgrade.sh`，由脚本完成所有校验和升级动作。
- 不再把手动解压、直接运行包内脚本作为推荐入口。
- 服务器 `.env`、数据库卷、上传文件和历史报告不属于升级包交付内容。

## 已知问题与风险

- 每次升级前仍需人工确认没有 `PENDING/RUNNING` 任务；本次不新增任务状态查询脚本。
- 回滚需要根据脚本输出的备份目录执行人工确认后的恢复命令。

## 下一步建议

1. 后续每个新版本沿用该说明和自动化脚本。
2. 新版本发布记录继续保存提交号、镜像标签、包 SHA、验收任务 ID 和报告路径。

## 下一会话首先阅读

- `docs/deployment/甲方生产离线升级操作说明.md`
- `deploy/production/deploy_offline_upgrade.sh`
- `README.md`

## 交接摘要

生产增量升级流程已固化为统一文档和一键脚本规范。
服务器只需上传升级包、SHA 文件和脚本后执行一条命令。
流程保留 `.env`、数据库卷、上传文件和历史报告。
已记录校验、升级、健康检查和回滚要求。
