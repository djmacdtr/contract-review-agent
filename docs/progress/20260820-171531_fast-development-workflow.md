# 任务进度：快速开发与分层测试工作流

## 基本信息

- 时间：2026-08-20 17:15:31 +08:00
- 状态：COMPLETED
- 任务类型：DOCS
- 代码目录：`D:\work\contract_review\contract-review-agent`
- 当前分支：`feat/final-compare-alignment`
- 当前提交：`f5a475e`
- 工作树状态：dirty；开始时已有其他会话的 OCR 错误处理代码和测试修改，本次仅修改协作规范并新增本记录

## 用户目标

减少每次功能迭代重复进行环境确认、全量回归、Docker 构建和真实外部调用造成的等待，将完整测试集中到里程碑或 PR 验收阶段。

## 本次完成

- 将新会话启动检查精简为 AGENTS、Git 状态、相关计划和最新相关交接记录。
- 同一会话不再重复读取未变化文档或确认已知可用的工具链环境。
- 建立快速开发、模块集成、完整验收三层测试策略。
- 将 Docker 全量、Alembic、完整前后端回归和真实 OCR/LLM 调用集中到验收门。
- 明确外部配置未变化且有近期成功证据时，不机械重复 probe。
- 允许同一里程碑的连续小改动合并生成一份进度记录。

## 修改文件

- `AGENTS.md`：增加快速启动、分层测试和外部服务调用策略。
- `docs/progress/README.md`：同步进度记录与测试粒度规则。
- `docs/progress/20260820-171531_fast-development-workflow.md`：新增本次交接记录。

## 接口、数据和配置变化

- API：无变化。
- 数据库/迁移：无变化。
- 配置：无运行配置变化，仅修改开发协作规范。
- 兼容性：无业务兼容性影响。

## 测试与验证

| 命令/检查 | 结果 | 关键数字或说明 |
|---|---|---|
| 文档规则一致性检查 | 通过 | AGENTS 与 progress README 均采用分层策略 |
| `git diff --check` | 待执行 | 只检查本次文档修改 |
| 业务测试 | 未执行 | 本次未修改业务代码；按快速开发策略无需全量回归 |

## Docker 与运行状态

- 本次未检查、重建、重启或停止 Docker 服务。
- 最终是否保持运行：未改变现有状态。

## 重要决策

- 默认使用快速开发模式，定向测试随改动执行，完整测试在验收门统一执行。
- 环境确认由“每次必做”改为“环境/依赖/配置变化或实际失败时再做”。
- 真实外部调用不再作为每个小迭代的固定前置步骤。

## 已知问题与风险

- 分层测试减少反馈时间，但可能延后发现跨模块回归；必须在 PR Ready、合并和交付前执行完整验收门。
- 当前工作树有其他会话未提交业务修改，本次未读取、覆盖或测试这些修改。

## 下一步建议

1. 后续会话默认按 AGENTS 的快速开发模式执行。
2. 在 FINAL_COMPARE 0.4.1 准备 PR Ready 时再集中执行完整验收。
3. 若某次修改涉及数据库、Docker、Adapter 协议或公开 API，自动提升到完整验收层级。

## 下一会话首先阅读

- `AGENTS.md`
- 当前任务相关计划
- 最新一份当前任务相关进度记录

## 交接摘要

日常开发默认只运行定向测试，不再重复环境确认和 Docker 全量回归。
模块级改动运行相关单元/集成测试。
完整 pytest、Docker、Alembic、健康冒烟和真实 OCR/LLM 集中到里程碑/PR验收。
环境和外部服务 probe 仅在配置变化、失败或状态未知时执行。
同一里程碑的小改动允许合并为一份进度记录。
