# FINAL_COMPARE V2 候选发现层离线收敛记录

状态：`PARTIAL`（候选发现的 41/17 已完成，误报发现与本地决策回放尚未完成）

## 范围

本轮仅调整 `FINAL_LOGICAL_V2` 的候选发现、业务坐标关系图、脱敏金标门禁和 cache-only dry-run。未调用 OCR/LLM，未创建或更新业务任务，未修改历史报告。

## 实现结果

- 表格坐标使用解析器提供的真实列号；无表头键值表按业务分组和子序号建立行键，支持空标签继承。
- 删除/新增候选使用单侧业务坐标；段落候选使用章节、语义和上下文锚点，不依赖跨文档物理段落号。
- 候选关系由连通分量生成，限制跨表格、跨字段和不可靠行/逻辑单元关联；候选集合保留原始物理位置。
- Canary/验收门禁不制造固定簇；首组缺少预期候选时返回安全错误。
- 脱敏金标仅保存拓扑和动作：41 个真实变化碎片、17 个逻辑组，以及绑定到 5 个等价组/1 个边界项的 12 个误报拓扑项；不保存合同正文。

## 首组 cache-only dry-run

- 原始差异：93
- 金标候选碎片：41
- 逻辑候选组：17
- 组大小分布：2 项 × 11、3 项 × 5、4 项 × 1
- 金标状态：`PARTIAL`；当前仅匹配 17/17 个真实变化组，等价组 0/5、边界项 0/1，误报实际发现 0/12
- 公开证据页码：247/247
- OCR 调用：0；LLM 调用：0；数据库写入：0
- 页面和差异证据仍由最新本地解析及持久化 PDF 缓存提供；本轮未进入 Canary 或正式任务。

## 离线检查

- 相关 V2 pytest：37 passed
- 变更范围 Ruff：通过
- 相关 Python compileall：通过
- `git diff --check`：通过（仅报告既有 LF/CRLF 提示）

## 未完成项

误报拓扑一一匹配未通过，`57` 项本地决策回放尚未执行；因此尚未执行外部 Canary、正式 FINAL_COMPARE 任务和三组控制台验收。需在后续受控阶段继续遵守单次创建和失败止损规则。
