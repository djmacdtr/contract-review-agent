# 任务进度：DRAFT_REVIEW 真实三文件闭环诊断

## 基本信息

- 时间：2026-08-25 17:20:29 +08:00
- 状态：PARTIAL
- 任务类型：FIX / DIAGNOSE
- 代码目录：`D:\work\contract_review\contract-review-agent`
- 当前分支：`feat/draft-review-multidoc`
- 当前提交：`797d06d`
- 工作树状态：dirty；进入本阶段前已有未提交修改，本阶段未覆盖、回退或清理；`.real-diagnostic-temp` 保留。

## 实际修改

- 加固临时真实诊断采集器：HTTP 响应读取增加可取消、可超时边界；响应和共享底层 transport 均在 finally 路径释放；安全事件逐条 flush；HTTP 客户端使用 `trust_env=False`；底层 transport 禁止自动重试。
- 补强运行器收尾：底层 transport 关闭异常也记录安全错误码并继续写入最终摘要，最后统一关闭指标文件。
- 新增一次性真实三文件运行器 `scripts/draft_review_real_diagnostic.py`：只加载现有 `.env` 网关配置，强制 OCR 关闭、4096 输出 token、结构纠错 1 次、payload 24000、拆分深度 3、每文档抽取预算 16，并只写安全结构指标。
- 保持结果留在内存中校验 `TaskResultData`；采集文件不写入 Key、合同正文、完整模型响应、签名 URL、任务 ID或模型字段。
- 完善事实抽取分批：表格按行形成抽取单元；行级 payload 不重复携带整表证据；完整表格仍保留单元格证据回查；数值候选发送给模型时只保留原值、分类和位置，去除内部哈希和跨度；单批候选上限调整为 160，使目标文档离线预检为 14 批，payload 上限仍为 24000。
- 保持非法 JSON 只二分当前批次；Schema、证据、envelope 和其他响应错误不触发二分；没有修改公开接口、请求参数、结果 Schema、数据库、FINAL_COMPARE 确定性文字差异逻辑或固定业务字段。
- 新增 Mock/fixture 覆盖正常响应、超时、取消、资源关闭、单次锁、异常不重试、非法 JSON 局部二分、拆分深度、调用预算及非 JSON 错误路径。

## 测试结果

| 命令/检查 | 结果 |
|---|---|
| 定向 pytest：采集器、LLM client、事实分批、DRAFT_REVIEW workflow、结果建议相关测试 | 通过，93 passed；1 个既有 LangGraph 弃用告警 |
| 变更 Python 文件 Ruff | 通过 |
| 变更 Python 文件定向 compileall | 通过 |
| `git diff --check` | 通过 |
| OCR、五文件任务、全仓 pytest、Docker 全量验收、数据库验收、前端视觉验收 | 未执行，按范围排除 |

离线目标文档分批预检结果：14 个抽取批，最大 payload 23934 字符，单批最大数值候选 159 个；未因此再次调用真实网关。

## 唯一真实三文件任务

仅使用了目标合同、同名模板和项目方案确认函三份文件。运行器配置为独立评审模式、OCR 关闭、`trust_env=False`，并创建了一次性锁；真实任务只启动 1 次，失败后没有重试或重新运行。

- 实际真实 HTTP LLM 调用总数：14。
- 逻辑 LLM 调用数：10。
- 按文件统计：目标合同 14 次；模板 0 次；项目方案确认函 0 次。
- 已完成：下载、解析、模板比对。
- 首个失败阶段：目标合同的事实抽取；模型响应未形成可解析 JSON，工作流在事实抽取阶段终止。
- 未开始：事实评审/映射、语义规划、数值校验、正式差异、AI 建议。
- 正式差异：未生成；左右证据及页码/段落/表格位置、字符级差异未形成正式结果。
- AI 建议：未生成。

安全指标文件和一次性锁仍保留在 `.real-diagnostic-temp`，未自动清理。安全指标中未发现 Key、授权头、模型响应正文或合同正文。

## 未完成项与下一步

- 本次真实任务未完成 DRAFT_REVIEW 全阶段闭环，因此不能宣称已生成甲方页面可展示的正式差异和 AI 建议。
- 本轮已完成首个失败阶段定位和针对性本地修复/回归；按照约束不再重复真实任务。
- 下一步需在新的明确授权下，复用本次安全运行器再进行一次受控真实三文件任务；若仍失败，只根据安全指标继续处理首个失败阶段，不扩展到五文件或其他验收范围。
