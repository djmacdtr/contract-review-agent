# 任务进度：FINAL_COMPARE 0.4.1 精度收口

## 基本信息

- 时间：2026-08-20 16:44:30 +08:00
- 状态：PARTIAL
- 任务类型：FIX / BUILD / TEST / DOCS
- 代码目录：`D:\work\contract_review\contract-review-agent`
- 当前分支：`feat/final-compare-alignment`
- 当前提交：`f5a475e`（本阶段提交尚未生成）
- 工作树状态：dirty；包含本阶段业务代码、测试、控制台、README、新计划/复核记录及本进度文件

## 用户目标

严格按计划完成 FINAL_COMPARE 0.4.1 精度收口：从失败测试开始，定点消除表格续行/阅读顺序误报，将法律条款和金额占位符单字符差异保留为 LOW 人工复核，补齐正负黄金集，完成全量、Docker、迁移和控制台验证，并在全部本地验证后只重跑一次 46 页真实任务。

## 本次完成

- 先新增表格续行失败测试，确认旧实现输出 2 项误报后再修改业务代码。
- 新增稠密表格行中间模型，按真实 `location.column` 兼容稀疏 OCR 单元格。
- 只合并同页同表、行号相邻、OCR 来源、主键为空、且仅名称/描述/说明/备注/规格等文本列非空的续行；编号、型号、数量、金额、日期、期限、比例、利率等受保护列不参与合并。
- 续行归一化同时用于结构化表格兼容门控和不兼容表格的阅读顺序 fallback，并保留合并前全部位置。
- `DiffItem` 增加可选 `review_reason`：`OCR_SINGLE_CHAR_VARIANCE`、`OCR_PLACEHOLDER_VARIANCE`、`OCR_READING_ORDER_VARIANCE`、`OCR_LOW_CONFIDENCE_VARIANCE`。
- 只有带明确 OCR 原因码的 LOW 非数值差异才作为 review-only；法律条款单字和金额占位符字符差异仍保留，不静默忽略。
- 数值、日期、比例和表格中的编号/数量/金额/日期变化保持 HIGH；主体单字、完整条款增删和表格名称变化均有精确类型、等级、位置及 token 断言。
- 控制台将业务差异与 OCR 人工复核项分区，OCR 区默认折叠并显示中文原因标签。
- workflow/rules 升级为 `0.4.1`；API 路由和 `schema_version=1.0` 保持不变。
- 完成本地、Docker、迁移、健康、持久化、日志脱敏及临时目录检查。
- 按约束只创建一次新的 46 页真实任务；任务失败后未重跑。

## 修改文件

- `app/comparison/models.py`：增加可选 OCR 复核原因协议。
- `app/comparison/reliable.py`：严格表格续行归一化、稀疏列处理、原因分类和数值等级保护。
- `app/comparison/engine.py`：review-only 判定改为依赖明确原因码。
- `app/workflows/final_compare.py`：workflow/rules 版本升级至 0.4.1。
- `frontend/src/api/types.ts`、`utils/labels.ts`、`views/TaskDetailView.vue`：原因类型、中文标签和 OCR 复核折叠区。
- `tests/unit/test_comparison.py`：正负黄金集和表格续行反例。
- `tests/unit/test_final_compare_workflow.py`：0.4.1 与 review-only 结果回归。
- `scripts/e2e_ocr_acceptance.py`：真实验收硬门槛收紧为 0 HIGH/MEDIUM、最多 3 LOW、`reliable=true`、`REVIEW_REQUIRED`。
- `README.md`：0.4.1 能力、测试数字和真实任务失败状态。
- `docs/plans/20260820_final-compare-0.4.1-precision-closure.md`：追加实际执行结果。
- `docs/progress/20260820-161727_sixteen-diff-review.md`、`20260820-162050_next-milestone-plan.md`：原样保护并纳入版本控制。
- `docs/progress/20260820-164430_final-compare-0.4.1-precision-closure.md`：本交接记录。

## 接口、数据和配置变化

- API：结果 `diff_items[]` 向后兼容增加可选 `review_reason`；无路由和请求变化。
- 数据库/迁移：无模型和迁移文件变化；`alembic check` 无缺失迁移，当前为 `0001_initial (head)`。
- 配置：无新增环境变量；真实任务前仅对临时 Compose 进程启用既有 OCR 和 fixture 配置，结束后已恢复默认 OCR 关闭状态。
- 兼容性：API schema 仍为 1.0；旧结果缺少 `review_reason` 时控制台仍按业务差异展示。

## 测试与验证

| 命令/检查 | 结果 | 关键数字或说明 |
|---|---|---|
| `pytest ...::test_adjacent_text_only_table_continuation_row_is_merged -q`（实现前） | 预期失败 | `1 failed`；旧实现产生 2 项表格误报 |
| comparison/workflow 定向测试 | 通过 | `43 passed, 1 warning` |
| 宿主机 `pytest -q` | 环境性部分通过 | `81 passed, 10 errors`；10 项均因宿主机不能解析 Compose 主机名 `postgres`，无业务断言失败 |
| 变更范围 `ruff check` | 通过 | `All checks passed!` |
| `npm run typecheck`、`npm run build` | 通过 | Vue 类型检查和 production build 成功；仅既有大 chunk 提示 |
| `docker compose config --quiet` | 通过 | Compose 配置有效 |
| `docker compose build api test` | 通过 | runtime/test 镜像从当前源码构建，镜像内前端构建通过 |
| `docker compose --profile tools run --rm test` | 通过 | `91 passed, 1 warning`；唯一 warning 为 LangGraph 上游未来弃用提示 |
| Alembic upgrade/check/current | 通过 | `No new upgrade operations detected`；`0001_initial (head)` |
| HTTP 冒烟 | 通过 | `/health`、`/ready`、`/docs`、`/console/` 均为 200 |
| 历史任务持久化 | 通过 | API/Worker 重建后旧任务仍可读取 |
| 日志敏感模式扫描 | 通过 | 0 命中；真实失败日志仅包含任务 ID 和安全错误码 |
| 临时目录 | 通过 | 真实任务失败后 `/tmp/contract-review` 为 0 条目 |
| `git diff --check` | 通过 | 无空白错误 |
| 浏览器视觉验收 | 未完成 | 浏览器控制能力返回无可用浏览器；未以 HTTP 200 冒充视觉通过 |

## 46 页真实任务

- 新任务：`tsk_01M0F5C0FB1SRY05XQP6AGKPW0`。
- 本阶段真实任务调用次数：1；任务内预期双方 external `auto`，失败后没有重跑。
- 预检：两份脱敏文件存在；Worker 0.4.1、OCR 配置完整；容器到 OCR 端口和只读 fixture 服务均可达。
- 结果：`FAILED / PARSING / 35`，`attempt_count=1`，错误码 `OCR_SERVICE_UNAVAILABLE`；约 6.3 秒进入终态。
- 未生成 `diff_items` 或 `comparison_diagnostics`，因此不能验证 0 HIGH、0 MEDIUM、0 数值误差、最多 3 LOW、`reliable=true` 和 `REVIEW_REQUIRED`。
- Worker 日志未包含服务地址、密钥、完整 URL 或合同正文；任务工作目录已清理。

## Docker 与运行状态

- API：运行，healthy，映射 `127.0.0.1:8000`，使用当前 0.4.1 镜像。
- Worker：运行，使用当前 0.4.1 镜像；真实验收后的临时 OCR 开关已恢复为默认关闭。
- PostgreSQL：运行且 healthy，命名卷 `contract-review-postgres-data` 保留。
- 控制台：HTTP 可访问；浏览器视觉验收未完成。
- fixture-server：已停止；只读样本未修改。
- 最终是否保持运行：API、Worker、PostgreSQL 保持运行。

## 重要决策

- 不使用全局字符 Counter 等价规则；相同字符集合但真实词序变化仍保留差异。
- 表格续行合并依赖结构证据和白名单文本列，不根据高相似度吸收编号、数量、金额或日期变化。
- `review_reason` 是 OCR review-only 的显式协议；普通 LOW 或仅位于 OCR 页面不自动改变总体结论。
- 唯一真实任务失败后遵守调用次数约束，不追加探测性解析请求或第二个任务。
- 因真实门槛和视觉验收未通过，PR 不标记 Ready，也不合并。

## 已知问题与风险

- external parser 在容器 TCP 可达后仍返回/触发 `OCR_SERVICE_UNAVAILABLE`；当前安全错误映射不会暴露 HTTP 状态或底层网络异常，需由外部服务或运行环境日志协同定位。
- 0.4.1 对历史 16 项的最终端到端精度尚未得到新真实结果证明。
- 当前没有可连接浏览器，OCR 复核折叠区、长文本和响应式布局尚未完成视觉确认。
- 严格续行规则对缺少“名称/描述/说明/备注/规格”等表头的异常表格选择保守不合并，可能保留 LOW 误报，但不会吞掉关键列变化。

## 下一步建议

1. 先由运行环境或 external parser 服务侧确认 `OCR_SERVICE_UNAVAILABLE` 的根因；不要未经用户再次授权重跑 46 页任务。
2. 恢复可连接浏览器后完成控制台视觉验收，重点检查 OCR 分区默认折叠、原因标签、多位置和长文本。
3. 获得新的真实调用授权后，再执行一次同样的 46 页验收脚本；只有硬门槛全部满足才将 PR 标记 Ready。
4. 在 0.4.1 真实验收完成前，不启动 DOCX→扫描 PDF 主黄金集、DRAFT_REVIEW、LLM 或 200 页性能阶段。

## 下一会话首先阅读

- `docs/plans/20260820_final-compare-0.4.1-precision-closure.md`
- `docs/progress/20260820-164430_final-compare-0.4.1-precision-closure.md`
- `docs/progress/20260820-161727_sixteen-diff-review.md`
- `app/comparison/reliable.py`
- `scripts/e2e_ocr_acceptance.py`
- `frontend/src/views/TaskDetailView.vue`

## 交接摘要

0.4.1 代码收口完成：严格 OCR 表格续行、显式复核原因、控制台分区和正负黄金集均已实现。
Docker 全量 91 项、构建、迁移、健康和日志检查通过；API/Worker/PostgreSQL 保持运行。
唯一真实任务 `tsk_01M0F5C0FB1SRY05XQP6AGKPW0` 在 PARSING 以 `OCR_SERVICE_UNAVAILABLE` 安全失败，未重跑。
因此最终 0/0/≤3 LOW、`reliable=true`、`REVIEW_REQUIRED` 门槛尚未验证，PR 必须保持 Draft。
当前无可连接浏览器，控制台视觉验收仍待完成。
