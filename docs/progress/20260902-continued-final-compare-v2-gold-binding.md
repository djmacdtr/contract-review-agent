# FINAL_COMPARE V2 金标绑定收敛续作

状态：`PARTIAL`

## 范围

本次仅继续完善 `FINAL_LOGICAL_V2` 的零调用金标捕获门禁。未调用 OCR/LLM，未创建正式任务，未修改业务数据库、历史报告、DRAFT_REVIEW、LEGACY、页码算法或公开接口。

## 已完成

- 连续删除块检查修正为显式绑定当前 run，消除 Ruff `B023` 闭包捕获问题。
- 金标捕获新增显式人工真实变化分组绑定文件入口。
- 捕获时仍先用双方摘要和位置将历史序号重新绑定到当前候选；不持久化动态 `candidate_id`。
- 缺少人工真实变化分组绑定、组集合漂移、序号重复、序号重叠或任务不匹配时安全停止，不写入金标。
- 金标 manifest 未被本次修改。

## 当前只读门禁

- 原始候选：`93`。
- 真实逻辑变化：`17` 组、`41` 个碎片。
- 等价候选：`5` 组、`11` 个碎片。
- 边界噪声：`1` 项。
- 等价误报实际匹配：`12/12`。
- 关系分组最大逻辑组：`4`；最大等价组：`3`。
- 金标门禁：阻断，原因 `CANDIDATE_GOLD_FALSE_POSITIVE_MISSING`；实际缺少的是现有 manifest 中 17 个真实变化拓扑指纹的人工绑定，不是候选数量或等价候选数量不足。
- 当前公开证据覆盖：`248/248`；原先 `247/247` 的口径来自待复核项尚未转为确认缺失风险的旧结果，不能在不改变页码算法的情况下强行宣称。
- OCR、LLM、数据库写入：均为 `0`。

## 验证

- V2 定向测试：`52 passed`。
- V2/比较/页码/风险相关测试：`128 passed`，`1` 个既有依赖弃用警告。
- 变更范围 Ruff：通过。
- 相关 Python `compileall`：通过。
- `git diff --check`：通过。

## 未完成与止损

尚未取得人工确认的 17 组真实变化序号集合，因此不生成或覆盖其拓扑指纹；外部 Canary、正式任务和三组验收继续停止。等人工绑定文件提供后，仍需重新做只读金标回放，并同时解决 `247/247` 与当前 `248/248` 的证据计数口径后，才可进入外部调用。
