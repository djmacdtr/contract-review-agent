# 语义规划正确性加固与客户报告收敛

时间：2026-08-25 12:16（Asia/Shanghai）

## 本阶段修改

- 为动态事实建立稳定的 `fact_id` 和跨批次索引；重复身份合并，来源或身份不一致时安全失败。
- 语义规划只接收评审整体、事实和决策均达到门槛且证据完整的 `ACCEPT` 事实；映射只接收 `MATCH + mapping review ACCEPT` 的合格关系。
- 概念、规则证据保留 `source_file_id + location`；数值 AST 使用 `fact_id + source_file_id`，通过索引和 `Decimal` 执行，拒绝旧式语义引用、未知事实和错误来源位置。
- 已确认的跨文件值冲突投影为现有 `DiffItem` 左右证据，并生成关联风险；`MISSING`、`UNCERTAIN` 和诊断语义结果不进入正式风险、通过项、规则检查或独立共识。
- 客户起草报告仅传递带差异的风险给建议模型，前端隐藏通过项和无差异技术风险；模型建议限制为一条短句并拒绝内部 ID、模型字段、置信度和技术术语。
- 数值候选保留开放式扫描，增加类型优先级、位置去重、结构性编号抑制和安全聚合指标。

## 合成回归

- 定向 pytest：`74 passed`，覆盖稳定事实索引、评审过滤、跨文件证据、限定 AST、数值类型和候选抑制、诊断边界、建议收敛及现有工作流契约。
- 定向 Ruff：通过。
- 变更 Python 定向 `compileall`：通过。
- 前端 `npm --prefix frontend run typecheck`：通过。
- `git diff --check`：通过。
- 未运行全仓 pytest、Docker、OCR、服务重启、MiniMax 或完整五文件任务。

## 唯一一次三文件真实诊断复验

- 输入范围：一个目标合同、一个模板和一份辅助资料；使用现有工作流入口，`LLM_SAME_MODEL_DIAGNOSTIC=true`、同一抽取模型、`LLM_MAX_OUTPUT_TOKENS=4096`、结构重试设为 0、OCR 关闭。
- 真实调用次数：`0`。流程完成下载、解析和模板确定性比较后，在事实抽取前因既有扩展表格安全门停止；因此抽取、评审、映射、语义规划、数值执行和建议生成均未发生真实模型调用。
- 安全聚合诊断：模板/目标单元数 `276/276`，对齐单元 `263`，基准/目标覆盖率约 `0.9771/0.9771`，有效基准覆盖率约 `0.9958`，检测到 `3` 个扩展表格；模板比较可靠性为真，但工作流的扩展表格门拒绝继续。
- `finish_reason`、usage、响应字符数和截断状态：均为 `N/A`（没有模型请求）。本记录未保存合同正文、事实值、完整响应、响应片段或密钥。
- 本次真实复验没有生成正式结果，不代表 PASS，也没有把诊断数据写入正式风险、通过项或独立共识。

## 下一步

在不改变确定性文字差异逻辑的前提下，需由后续阶段决定如何处理该真实样本的既有扩展表格安全门（例如使用已获批准的无扩展表格样本复验，或单独评估表格能力）；之后再进行一次完整链路真实诊断，不扩展辅助资料数量。
