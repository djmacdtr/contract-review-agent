# 任务进度：甲方原型、无等级风险与双页面审查

## 基本信息

- 时间：2026-08-21 10:29:04 +08:00
- 状态：COMPLETED
- 任务类型：REVIEW / DOCS
- 代码目录：`D:\work\contract_review\contract-review-agent`
- 当前分支：`feat/draft-review-multidoc`
- 当前提交：`fd1bdb4`
- 工作树状态：dirty；阶段 B 业务修改和前序进度未提交，本次未覆盖它们，仅新增计划与本记录

## 用户目标

根据甲方提供的合并 Tab 原型，取消风险高、中、低评级，并规划实际交付所需的起草检查和放款比对两个独立嵌入页面。

## 本次完成

- 完整读取甲方 `AI合同智能对比_审查报告样板_合并版_V2.html` 的结构、样式、两个阶段内容和 Tab 交互。
- 核对现有 Vue 路由、任务详情、结果类型、后端 DiffItem、任务统计字段和数据库等级计数依赖。
- 确认风险模型改为“风险 / 人工复核 / 通过”，风险内部不分等级。
- 确认 `diff_items` 是技术证据，只有确认的不合规差异才生成 `risk_items`；OCR 低置信度等不确定事项进入 review/warning。
- 规划结果 Schema 2.0、任务新统计字段及旧等级列兼容策略。
- 规划同一 SPA 内两个无 Tab 独立报告路由，测试控制台继续保留。
- 明确原型中的印章功能、起草签章空白和固定资料类型不符合当前范围，不直接照搬。
- 新增完整实施计划和验收标准。

## 修改文件

- `docs/plans/20260821_ungraded-risk-and-embedded-pages.md`：无等级风险、双页面、迁移和实施计划。
- `docs/progress/20260821-102904_prototype-risk-ui-review.md`：本次审查交接记录。
- 未修改业务代码、数据库、迁移、配置、Docker 服务或甲方原型文件。

## 接口、数据和配置变化

- API：本次未实现；计划将结果升级为 Schema 2.0，移除 severity 和等级统计。
- 数据库/迁移：本次无变化；计划新增 risk_count/review_count，旧等级列暂时兼容保留。
- 配置：本次无变化；iframe 模式后续可能新增允许嵌入来源配置。
- 兼容性：本次无变化；正式实施时属于破坏性结果契约变更，需要版本升级和旧任务适配。

## 测试与验证

| 命令/检查 | 结果 | 关键数字或说明 |
|---|---|---|
| 甲方原型源码审查 | 通过 | 1 个 HTML、2 个 Tab 阶段、起草 8 模块、放款 4 模块 |
| 浏览器视觉打开 | BLOCKED | 浏览器安全策略禁止 `file://`；未绕过限制，因此不宣称像素级视觉验收 |
| 现有 severity 依赖检索 | 通过 | 后端比较、工作流、结果 Schema、数据库、前端和测试均有依赖，属于跨层变更 |
| Vue 路由核对 | 通过 | 当前只有通用 TaskDetail，没有两个正式嵌入报告路由 |
| 文档 diff 检查 | 通过 | `git diff --check` 无错误，本次仅新增 Markdown |
| 业务测试 | 未执行 | 本次未修改业务代码 |

## Docker 与运行状态

- 本次未启动、停止、重建或检查 Docker 服务。
- 沿用上一交接：API、Worker、PostgreSQL 保持运行。
- 最终是否保持运行：是，未改变现有状态。

## 重要决策

- 所有确认不合规项都是风险，不设置严重程度等级。
- 无法确认是否违规的解析/OCR/对齐问题必须单独进入人工复核，不能为了“全部是风险”而误判。
- 两个正式页面使用不同 URL 和不同页面组件，不使用 Tab；仍共用构建产物和基础组件。
- 甲方系统应先通过 API 创建任务，再以 task_id 嵌入只读报告页面，不能在 iframe URL 中传合同下载地址。
- 印章相关内容不进入当前交付范围。

## 已知问题与风险

- 当前 `severity` 深入比较模型、规则、工作流、数据库、API、前端和大量测试，不能只隐藏前端标签。
- 现有任务表只有等级计数，需要迁移或兼容映射。
- iframe 的父域名、尺寸、自适应高度和 CSP 白名单尚未由甲方确认。
- 原型内容包含与现有范围冲突的印章和签章空白示例，后续实现必须按正式需求而不是示例文案判断。
- 本轮未完成视觉截图验收；正式页面实现后需要在可访问的 localhost 页面进行浏览器验收。

## 下一步建议

1. 先提交当前阶段 B 模板确定性基线，建立干净检查点。
2. 实施里程碑 0.3.1：Schema 2.0、无等级风险、数据库兼容迁移和全层测试。
3. 使用真实黄金样本完成 26 差异 + 3 扩展表格的二元风险标注和规则回归。
4. 建立两个独立嵌入页面壳，再实现 LLM Client，避免模型输出继续依赖旧等级结构。
5. 向甲方确认 iframe/微前端方式、父页面 origin、目标宽高和是否需要 postMessage 自适应高度。

## 下一会话首先阅读

- `AGENTS.md`
- `docs/plans/20260821_ungraded-risk-and-embedded-pages.md`
- `docs/progress/20260821-095600_draft-template-baseline.md`
- `docs/progress/20260821-102904_prototype-risk-ui-review.md`
- `app/comparison/models.py`
- `app/schemas/results.py`
- `app/db/models/entities.py`
- `frontend/src/router.ts`
- `frontend/src/api/types.ts`

## 交接摘要

甲方风险模型正式改为不分等级，确认不合规即风险。
不确定的 OCR/解析/对齐事项单列人工复核，不能冒充风险。
实际交付为起草和放款两个无 Tab 独立报告页面，共用同一 SPA 和组件。
原型中的印章、起草签章空白和固定资料类型不照搬。
下一里程碑应先改 Schema 2.0 和数据库统计，再做双页面和 LLM 接入。
