# FINAL_COMPARE V2 金标误报发现与本地决策回放

状态：`PARTIAL`

## 本轮范围

本轮仅完善 `FINAL_LOGICAL_V2` 的零调用金标门禁、脱敏拓扑匹配和本地决策回放。没有调用 OCR/LLM，没有创建任务，没有写入业务数据库，也没有修改 DRAFT_REVIEW、LEGACY、历史报告或公开接口。

## 离线验证

- V2 定向测试：`37 passed`。
- 变更范围 Ruff：通过。
- 相关 Python `compileall`：通过。
- `git diff --check`：通过；仅有既有换行格式提示。
- cache-only dry-run：OCR `0`、LLM `0`、数据库写入 `0`。

## 当前真实缓存金标摘要

- 当前确定性候选：`93`。
- 已匹配真实变化：`17` 组、`41` 个碎片。
- 已发现等价误报：`0` 组、`0` 个碎片；金标要求 `5` 组、`11` 个碎片。
- 已发现边界噪声：`0` 项；金标要求 `1` 项。
- 误报拓扑绑定：金标 `12` 项已逐项绑定到等价组/边界组；当前实际匹配 `0/12`。
- 公开页码证据：`247/247` 覆盖，缺失 `0`。
- 金标门禁结果：`BLOCKED`，首个安全错误码为 `CANDIDATE_GOLD_FALSE_POSITIVE_MISSING`。

金标校验现在使用脱敏拓扑指纹、动作和碎片数逐组一一匹配，不再接受只声明误报数量的通过。`false_positives` 的组引用和拓扑指纹也会进行一致性校验。本地回放只有在完整金标通过后才执行，因此本轮回放状态为未执行/被金标门禁阻断。

## 未完成项

- 当前缓存尚未产生金标要求的 `EQUIVALENT_NO_CHANGE` 五组和 `BOUNDARY_NOISE` 一项；不能宣称 `41/17/12` 完整通过。
- `57` 项正式风险、`0` 项待复核及租金支付计划缺失的本地决策回放尚未启动。
- 四类外部 Canary 和正式 FINAL_COMPARE 任务均未执行。

下一步应继续从当前真实候选完善确定性发现/分类，直到金标一一匹配和本地回放全部通过；在此之前保持外部调用门禁关闭。
