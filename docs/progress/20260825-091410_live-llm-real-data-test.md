# 任务进度：真实脱敏数据 LLM 测试

## 基本信息

- 时间：2026-08-25 09:14:10 +08:00
- 状态：PARTIAL
- 任务类型：TEST / DIAGNOSE
- 代码目录：`D:\work\contract_review\contract-review-agent`
- 当前分支：`feat/draft-review-multidoc`
- 当前提交：`581ea08`
- 工作树状态：dirty；保留既有未提交开发修改，本次仅新增测试记录

## 用户目标

在 `OpenAIContractLlmClient` 显式禁用环境代理后，使用脱敏真实数据执行一次真实 LLM 测试。

## 本次完成

- 使用宿主机 `.venv` 和当前 `.env` 执行真实 `/v1/models` 探测，确认直连网关成功。
- 本地解析仓库外的脱敏真实样本 `项目方案确认函.docx`；输入为 10 个块、1 张表、952 个正文字符和 1 个 LLM 分块。
- 使用生产 `OpenAIContractLlmClient.extract_facts()` 将该结构化分块发送给真实 LLM 网关，并由项目 Schema 与证据校验入口接收结果。
- 未回显或落盘 API Key、合同正文、事实原值、证据原文或完整模型响应；未调用 OCR、下载器、数据库或任务工作流。

## 修改文件

- `docs/progress/20260825-091410_live-llm-real-data-test.md`：记录真实调用结果与当前阻塞。

## 接口、数据和配置变化

- API：无。
- 数据库/迁移：无。
- 配置：无持久修改；读取当前 `.env`，未输出凭据和完整网关地址。
- 兼容性：无代码变化；测试使用当前生产客户端的 `trust_env=False` 行为。

## 测试与验证

| 命令/检查 | 结果 | 关键数字或说明 |
|---|---|---|
| 生产客户端 `probe_models()` | 通过 | 首次约 345 ms；返回 5 个模型条目 |
| 网关模型登记核对 | 不匹配 | 返回 `Qwen3.8-27B`、`MiniMax-M2.7`、`bge-m3`、`bge-reranker-base`、`DeepSeek-V4-Flash-0731`；当前抽取模型 `GLM-5.2` 和评审模型 `GLM-5.2-reviewer` 均不在列表中 |
| 脱敏真实 DOCX 本地解析 | 通过 | 10 blocks、1 table、952 characters、1 chunk |
| 生产客户端真实 `extract_facts()` | 失败 | 225,483 ms 后返回 `LLM_INVALID_JSON`；HTTP 请求已经到达并获得响应，但允许的结构纠错后仍不是有效 JSON |
| Schema/证据校验 | 未进入 | 响应在 JSON 解析阶段失败，未形成可校验的 `DocumentFactExtraction` |

说明：首次真实抽取进程约 3 分钟后结束，但工具会话未保留其标准输出，不能引用为有效测试结论。为取得可复核结果，使用相同 952 字符输入重复执行一次并正确保留输出；两次均未输出合同内容。

## Docker 与运行状态

- API：未操作。
- Worker：未操作。
- PostgreSQL：未操作。
- 控制台：未操作。
- 最终是否保持运行：本任务未改变任何服务状态。

## 重要决策

- 按既定真实验收顺序选择体量最小的脱敏辅助资料，只测试单文档事实抽取，不启动存在模板比较阻塞的完整任务。
- 真实输出只记录安全元数据；因 JSON 解析失败，不打印模型原始响应来避免合同衍生内容泄露。
- `/v1/models` 已快速成功，说明本次 `trust_env=False` 修复解决了先前环境代理导致的 502/超时路径；当前阻塞已转移到模型登记和结构化输出质量。

## 已知问题与风险

- 当前 `.env` 的 `GLM-5.2` 与 `GLM-5.2-reviewer` 均未出现在实时模型列表，配置与网关当前登记不一致。
- 当前抽取请求连续结构纠错后仍返回无效 JSON，不能生成事实结果，也未进入证据完整性校验。
- 单次 952 字符真实抽取耗时约 225.5 秒，接近 300 秒客户端超时，不适合直接扩展到完整多文件任务。
- 本次只验证抽取阶段；未执行独立评审、事实映射、映射评审或 Advice。

## 下一步建议

1. 先由项目负责人确认应使用实时列表中的哪个文本模型作为抽取模型，并选择另一个真实文本模型作为独立评审模型。
2. 用确认后的模型执行一条不含合同正文的严格 JSON 最小请求，检查 JSON 遵循率、实际模型字段和延迟。
3. 最小结构化请求通过后，再对同一份 952 字符脱敏样本重跑一次事实抽取；通过 Schema 和证据校验后才扩展到多文件流程。

## 下一会话首先阅读

- `app/adapters/llm/openai_client.py`
- `app/adapters/llm/schemas.py`
- `docs/progress/20260825-085011_llm-disable-env-proxy.md`
- `docs/progress/20260825-091410_live-llm-real-data-test.md`

## 交接摘要

禁用环境代理后，真实 `/v1/models` 已成功，首次约 345 ms。实时列表有 5 个模型，但不包含当前配置的 `GLM-5.2` 或 `GLM-5.2-reviewer`。脱敏真实 `项目方案确认函.docx` 本地解析为 952 字符、1 个分块；真实抽取约 225.5 秒后因连续返回无效 JSON 而失败为 `LLM_INVALID_JSON`。未调用 OCR、下载器或完整任务，未输出凭据或合同正文。
